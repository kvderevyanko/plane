# CAD source

Source models live here; output belongs only in `build/`. `common/` contains
calibration geometry, while `wing/`, `fuselage/`, and `tail/` are reserved for
aircraft assemblies. Every model must use millimetres.
